# ── Core async HTTP ─────────────────────────────────────────────────────────
aiohttp>=3.9.0

# ── Telegram bot ─────────────────────────────────────────────────────────────
python-telegram-bot[job-queue]>=21.0

# ── Polymarket CLOB client (official SDK) ────────────────────────────────────
# Install manually: pip install py-clob-client
# GitHub: https://github.com/Polymarket/py-clob-client
# Required for: order placement, balance check, API key derivation
# Implements the RobotTraders notebook auth pattern:
#   auth_client = ClobClient(CLOB_API, key=PK, chain_id=137, funder=ADDRESS)
#   creds = auth_client.derive_api_key()
#   auth_client.set_api_creds(creds)
# py-clob-client>=0.17.0   (uncomment after manual install)

# ── Environment & config ─────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Retry logic ──────────────────────────────────────────────────────────────
tenacity>=8.2.3

# ── HTTP (sync fallback, used in some utilities) ─────────────────────────────
requests>=2.31.0

# ── Numerical / statistics ───────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0

# ── Web3 / Polygon (for CLOB wallet signing) ─────────────────────────────────
# web3>=6.0.0   (uncomment when using live trading)
